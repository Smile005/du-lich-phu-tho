export type News = {
    id: string,
    title: string,
    description?: string,
    tags?: string[],
    views?: string,
    author?: string,
    date?: string,
    image: string,
    news: string
}
